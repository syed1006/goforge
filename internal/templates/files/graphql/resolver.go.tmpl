// Package graph holds the GraphQL resolver root for {{ .ProjectName }}.
package graph

// Resolver carries application-level dependencies (services, storage) that the
// generated resolvers will close over. Add fields here as your application grows.
type Resolver struct{}
